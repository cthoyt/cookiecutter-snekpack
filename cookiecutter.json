{
  "package_name": "",
  "package_name_stylized": "",
  "short_description": "",
  "author_name": "",
  "author_github": "",
  "author_email": "",
  "github_organization_name": "",
  "github_repository_name": "",
  "command_line_interface": [
    true,
    false
  ],
  "gitlab": [
    false,
    true
  ],
  "runner": [
    "just",
    "tox",
    "nox"
  ],
  "citation_file": [
    false,
    true
  ],
  "__not_charlie": "{% if cookiecutter['author_name'] == 'Charles Tapley Hoyt' %}{% else %}true{% endif %}",
  "__runner": "{% if cookiecutter['runner'] == 'tox' %}tox -e{% elif cookiecutter['runner'] == 'just' %}just{% else %}nox -s{% endif %}",
  "__runner_uv": "{% if cookiecutter['runner'] == 'tox' %}--with tox-uv tox -e{% elif cookiecutter['runner'] == 'just' %}--from rust-just just{% else %}nox -s{% endif %}",
  "__runner_pip": "{% if cookiecutter['runner'] == 'tox' %}tox tox-uv{% elif cookiecutter['runner'] == 'just' %}rust-just{% else %}nox[uv]{% endif %}",
  "__runner_install_uv": "uv tool install {% if cookiecutter['runner'] == 'tox' %}tox --with tox-uv{% elif cookiecutter['runner'] == 'just' %}rust-just{% else %}nox[uv]{% endif %}",
  "__runner_install_pip": "python3 -m pip install {% if cookiecutter['runner'] == 'tox' %}tox tox-uv{% elif cookiecutter['runner'] == 'just' %}rust-just{% else %}nox[uv]{% endif %}",
  "__runner_tests": "{% if cookiecutter['runner'] == 'tox' %}py{% elif cookiecutter['runner'] == 'just' %}test{% else %}tests{% endif %}",
  "__gh_slug": "{{ cookiecutter.github_organization_name }}/{{ cookiecutter.github_repository_name }}",
  "__package_name_dashed": "{{ cookiecutter['package_name'].replace('_', '-') }}"
}
